ame: Script Injection
# on open an issue
on:
  issues:
    types: [opened]
jobs:
  inject:
    runs-on: ubuntu-latest
    steps:
      - name: Inject
        run: |
          issue_number=$(jq --raw-output .issue.number "$GITHUB_EVENT_PATH")
          echo "issue_number=$issue_number"
